<h3><%= 'Editing user'[] %> <%=h @user.login %></h3>

<%= error_messages_for :user %>

<% form_for(:user, :url => user_path(@user), :html => { :method => :put }) do |f| %>
  <%= render :partial => 'form', :object => f %>
  <p><%= submit_tag "Update"[] %></p>
<% end %>
